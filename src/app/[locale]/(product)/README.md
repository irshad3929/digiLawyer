## Group Route

Group route are used to group routes together.
They are useful for creating a layout that is shared across multiple routes
Without having to worry of the extra path segment if wrapped in ()
